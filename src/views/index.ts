import { App } from '@slack/bolt';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const register = (app: App) => {
  // View submission handlers would be registered here if needed
};

export default { register };
